// Example 1: simple rainfall through one dam

let rain_today = rain(50mm);   // 50 units of rainfall today

dam d1 = 0.8;                  // implicit reduce: passes 80% of inflow

rain_today -> d1;

output d1;                     // per-day flow at d1, and multi-day if 'days' set
