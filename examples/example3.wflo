let inflow = 100;
dam t = threshold(50, 0.3, 0.9);
level t = 40;       // below threshold → low=0.3

inflow -> t;
output t;           // expect 30

level t = 60;       // now above → high=0.9
output t;           // expect 90 (overwrites previous level)
